{"name": "pusher-chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix Pusher && npm run build --prefix Pusher",
    "heroku config": " set NODE_MODULES_CACHE=false"
  },
  "engines": {
    "node": "16.13.1"
  },
  "author": "bhautik davariya",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "path": "^0.12.7",
    "pusher": "^5.1.0-beta",
    "pusher-js": "^7.1.0-beta"
  }
}
